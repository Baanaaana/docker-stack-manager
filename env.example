# Docker Stack Manager Configuration

# The base URL of your Portainer instance (include https:// or http://)
PORTAINER_URL=https://portainer.example.com

# Portainer API access token (generate this in Portainer -> Settings -> Users -> Access tokens)
PORTAINER_TOKEN=PASTE_ACCESS_TOKEN_HERE

# Name of the Docker stack you want to manage (case-sensitive)
# Use "ALL" to display all stacks and their containers
STACK_NAME=esphome

# IP Access Control
# Comma-separated list of allowed IP addresses or CIDR ranges
# Examples: 
#   Single IP: 192.168.1.100
#   Multiple IPs: 192.168.1.100,10.0.0.50
#   CIDR range: 192.168.1.0/24
#   Mixed: 192.168.1.100,10.0.0.0/8
# Leave empty to allow all IPs (not recommended for production)
ALLOWED_IPS=192.168.1.100,10.0.0.50